# Pinned to the exact versions of the environment the model artifacts were
# trained and evaluated in (Python 3.11.9). Reproducibility rule:
# training environment == evaluation environment == runtime environment.
# `models/*.joblib` embeds scikit-learn estimators; loading them with a
# different major scikit-learn version is not guaranteed to work.
# NOTE (2026-09-14): 1.9.1 -> 1.9.0. The artifacts and every committed report
# were regenerated under 1.9.0 after the build machine's Application Control
# policy began blocking 1.9.1's compiled extensions (see FINAL_VERIFICATION.md
# §9). All metrics were re-verified identical under 1.9.0.
numpy==2.4.6
scipy==1.17.1
scikit-learn==1.9.0
pandas==3.0.5
joblib==1.6.0
PyYAML==6.0.3
Flask==3.1.3
gunicorn==26.2.0
requests==2.34.2
python-dotenv==1.2.3
pytest==9.1.1
httpx==0.28.1

# The assignment's suggested stack (FastAPI, Pydantic) is intentionally NOT
# included: this backend is Flask (see docs/decision-log.md #13). A FastAPI
# port would be a mechanical rewrite of backend/app/api/app.py, whose
# endpoint docstrings document every schema.
# fastapi==0.115.x
# pydantic==2.x
